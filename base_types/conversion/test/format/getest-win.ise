test

	xformat

default

	class ("TEST_[A-Z0-9_]*")
	feature ("test_[a-z0-9_]*")
	prefix ("X")
	testgen (".")
	compile ("(ec -ace ise.ace & cd EIFGEN\W_code & finish_freezing -silent) > xcompile.log 2>&1")
	execute ("cd EIFGEN\W_code & xformat")

cluster

	format: "$SAFE_EBT\conversion\test\format"

end
